# ============================================================================
# myRSI (single-org, self-hosted) — environment configuration
# Copy to `.env` and fill in. Never commit your real `.env`.
# ============================================================================

# --- Runtime ---------------------------------------------------------------

NODE_ENV=production
PORT=3000

# Public origin of this deployment, e.g. https://yourdomain.com (no trailing slash).
# Used for Discord announcement deep links, Discord scheduled-event locations, the
# origin advertised to alliance federation peers, and the intel self-feed shortcut.
#
# ENV WINS: this OVERRIDES the appUrl carried in the database `settings.systemConfig`
# row. That row is org data and survives a database restore, so a deployment migrated
# to a new domain would otherwise keep emitting links to the OLD one — set this and the
# stale value is ignored (the boot log prints which source won). Left blank, the app
# falls back to that stored value, then to http://localhost:3000.
#
# Left as the literal placeholder it is IGNORED, so half-configuring it can't publish
# links to a domain you don't own.

APP_URL=

# Logger verbosity: debug | info | warn | error | silent

LOG_LEVEL=info

# --- Supabase (database + realtime) ----------------------------------------
# Project URL and keys from Supabase Dashboard → Project Settings → API.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key

# Service-role key bypasses RLS — server-only, never expose to the client.
# Required: the server cannot reach the DB without it.

SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Project JWT secret (Dashboard → Project Settings → API → JWT Secret).
# The server mints per-user realtime tokens with it so clients can subscribe
# to the PRIVATE realtime broadcast channels (live updates). Without it,
# realtime is disabled (fail-closed) — the app still works via manual
# refreshes, but live updates across browsers stop.

SUPABASE_JWT_SECRET=

# --- Security --------------------------------------------------------------
# Dedicated JWT signing secret (24-hour session tokens). REQUIRED — the server
# refuses to start without it. Do NOT reuse SUPABASE_SERVICE_ROLE_KEY here: the
# service-role key bypasses RLS, so reusing it means one leak compromises both
# session signing and the database. Generate: openssl rand -hex 32

JWT_SECRET=

# How many reverse-proxy hops sit in front of the app (Express `trust proxy`).
# Controls how the client IP is resolved for rate limiting + abuse blocking.
# 0 (default, SECURE) = Node is directly exposed with no proxy — use the real TCP
#     peer address, which a client cannot spoof. Safe out of the box.
# 1 = ONE reverse proxy in front (e.g. the TLS terminator from the deployment guide).
# 2+ = deeper chains (e.g. CDN -> load balancer -> app).
# Set this to your actual proxy count ONLY when you run a proxy. Too LOW behind a
# proxy makes everyone share the proxy's IP (coarse limits); too HIGH (or any value
# with no proxy) lets a client spoof X-Forwarded-For and evade IP-keyed limits.

TRUST_PROXY_HOPS=0

# Set to 1 ONLY if this origin is reachable exclusively through Cloudflare
# (Cloudflare Tunnel, or a firewall allow-listing Cloudflare IPs). The server
# will then trust the CF-Connecting-IP header for the real client IP. If the
# origin is directly reachable, leave unset — a direct caller could spoof the
# header to evade rate limits or frame another IP into the abuse blocker.

TRUST_CF_PROXY=

# REQUIRED (production): AES-256-GCM encryption at rest for admin-entered secrets
# (Discord tokens, LiveKit keys, Gemini key) stored in the `settings` table. The
# server REFUSES TO BOOT in production without it, and saving a secret fails
# closed if it is unset — secrets are never stored in plaintext.
# Generate: openssl rand -hex 32
# WARNING: once set and secrets are saved, do NOT change or remove it —
# previously-encrypted values become unrecoverable.

SECRETS_ENCRYPTION_KEY=

# --- Discord (OAuth login + bot) -------------------------------------------
# From the Discord Developer Portal (https://discord.com/developers).
# OAuth2 → add redirect:  ${APP_URL}/  (the app posts its own redirect_uri).
# ENV WINS: if these are set they OVERRIDE whatever is saved in
# Admin → Discord settings. Leave them blank to manage the credentials from the
# admin console — otherwise rotating a leaked secret in the UI has no effect,
# because the stale env value keeps taking precedence.

DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# Bot token + your server (guild) ID — needed for role sync, member lookup,
# channel/event posting. Optional if you don't use Discord bot features.

DISCORD_BOT_TOKEN=
DISCORD_GUILD_ID=

# --- Web Push (PWA notifications) ------------------------------------------
# Generate a keypair: npx web-push generate-vapid-keys

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@yourdomain.com

# --- Optional integrations -------------------------------------------------
# UEX Corp API key (Star Citizen market data), if used.

UEX_API_KEY=

# UEX API base URL. Leave blank for the default, https://api.uexcorp.space/2.0.
#
# Set this if catalog sync fails with a 403 carrying a Cloudflare "Just a moment..."
# page while a plain `curl` with the same token from the same box succeeds. That's the
# upstream CDN challenging your server's IP, not a bad key, and it is per-host: the
# known alternate below is the same API on a different zone.
#
#   UEX_API_BASE=https://api.uexcorp.uk/2.0
#
# Any http(s) URL works, so this can also point at your own outbound proxy. Include the
# API version segment; no trailing slash.

UEX_API_BASE=

# Milliseconds between UEX requests (default 600). UEX allows ~120 req/min; the default
# keeps a full catalog sync at roughly 100 req/min. Raise it if you get HTTP 429.

UEX_REQUEST_DELAY_MS=

# Google Gemini key for AI features. Also settable in Admin → AI settings, but
# as with Discord above, a value set HERE wins over the admin-console one.

GEMINI_API_KEY=

# LiveKit (voice radio). Leave blank to disable radio features. Also settable in
# Admin → Radio settings; as above, a value set HERE wins over the console one.

LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
LIVEKIT_URL=wss://your-livekit-host